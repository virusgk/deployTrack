<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <system.webServer>
        <httpProtocol>
            <customHeaders>
                <add name="X-Forwarded-Host" value="{HTTP_HOST}" />
            </customHeaders>
        </httpProtocol>
        <rewrite>
            <rules>
                <rule name="NextJsReverseProxy" stopProcessing="true">
                    <match url="(.*)" />
                    <action type="Rewrite" url="http://localhost:9002/deploytrack/{R:1}" />
                </rule>
            </rules>
        </rewrite>
    </system.webServer>
</configuration>
